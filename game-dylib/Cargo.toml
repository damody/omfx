
[package]
name = "game_dylib"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
omfx = { path = "../game", default-features = false }

[features]
default = ["omfx/default"]
dylib-engine = ["omfx/dylib-engine"]
