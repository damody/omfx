
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
omfx = { path = "../game", optional = true }
log = "0.4"
simplelog = "0.12"

[features]
default = ["omfx"]
dylib = ["fyrox/dylib"]
